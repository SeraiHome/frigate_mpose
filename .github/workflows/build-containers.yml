# Build Serai container images and push to GHCR
# Runs on self-hosted prod ARM64 runner (3 OCPU, 18GB RAM)
# Builds: frigate_mpose (RPi target) + privacy-proxy sidecar

name: Build Container Images

on:
  workflow_dispatch:
    inputs:
      frigate_tag:
        description: 'Frigate image tag'
        required: false
        default: 'latest'
      privacy_proxy_tag:
        description: 'Privacy proxy image tag'
        required: false
        default: 'latest'

permissions:
  contents: read
  packages: write

env:
  REGISTRY: ghcr.io
  FRIGATE_IMAGE: ghcr.io/seraihome/frigate_mpose
  PRIVACY_PROXY_IMAGE: ghcr.io/seraihome/privacy-proxy

jobs:
  build-frigate:
    name: Build frigate_mpose (RPi)
    runs-on: [self-hosted, arm64, serai-prod]
    steps:
      - name: Checkout frigate_mpose
        uses: actions/checkout@v4

      - name: Log in to GHCR
        run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin

      - name: Free disk space
        run: |
          docker system prune -af 2>/dev/null || true
          echo "Disk free: $(df -h / | tail -1 | awk '{print $4}')"

      - name: Generate version
        run: echo 'VERSION = "0.17.0-'$(git log -1 --pretty=format:"%h")'"' > frigate/version.py

      - name: Build frigate_mpose RPi image via buildx bake
        run: |
          TAG="${{ github.event.inputs.frigate_tag || 'latest' }}"
          echo "Building frigate_mpose:${TAG} via bake..."
          docker buildx bake --file=docker/rpi/rpi.hcl rpi \
            --set rpi.tags=${{ env.FRIGATE_IMAGE }}:${TAG} \
            --load
          echo "Build complete: $(docker images ${{ env.FRIGATE_IMAGE }}:${TAG} --format '{{.Size}}')"

      - name: Push frigate_mpose to GHCR
        run: |
          TAG="${{ github.event.inputs.frigate_tag || 'latest' }}"
          docker push ${{ env.FRIGATE_IMAGE }}:${TAG}
          echo "Pushed ${{ env.FRIGATE_IMAGE }}:${TAG}"

  build-privacy-proxy:
    name: Build privacy-proxy
    runs-on: [self-hosted, arm64, serai-prod]
    steps:
      - name: Checkout frigate_mpose
        uses: actions/checkout@v4

      - name: Log in to GHCR
        run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin

      - name: Build privacy-proxy image
        run: |
          TAG="${{ github.event.inputs.privacy_proxy_tag || 'latest' }}"
          echo "Building privacy-proxy:${TAG}..."
          docker build -t ${{ env.PRIVACY_PROXY_IMAGE }}:${TAG} serai-edge-setup/privacy-proxy/
          echo "Build complete: $(docker images ${{ env.PRIVACY_PROXY_IMAGE }}:${TAG} --format '{{.Size}}')"

      - name: Push privacy-proxy to GHCR
        run: |
          TAG="${{ github.event.inputs.privacy_proxy_tag || 'latest' }}"
          docker push ${{ env.PRIVACY_PROXY_IMAGE }}:${TAG}
          echo "Pushed ${{ env.PRIVACY_PROXY_IMAGE }}:${TAG}"

  # Config-agent is built from the serai-edge-setup repo.
  # Trigger its build workflow separately, or build it in the EDI pipeline.
